{
  "name": "upfront",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "frontend",
    "backend",
    "admin"
  ],
  "scripts": {
    "dev": "concurrently -n FRONTEND,BACKEND,ADMIN -c cyan,green,magenta \"npm run dev -w frontend\" \"npm run dev -w backend\" \"npm run dev -w admin\"",
    "dev:frontend": "npm run dev -w frontend",
    "dev:backend": "npm run dev -w backend",
    "dev:admin": "npm run dev -w admin",
    "build": "npm run build -w frontend && npm run build -w admin",
    "start": "npm run start -w backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "dependencies": {
    "react-router-dom": "^7.14.2"
  }
}
